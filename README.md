# spy-frontend

Vite + React frontend, replacing the Next.js frontend that used to live in the
`spy` monorepo (`apps/web`).

Work happens on feature branches merged via pull request — `main` is not
pushed to directly.
